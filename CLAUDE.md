# austincv.com — Project Context for Claude

Personal website for Austin Chungath Vincent. A static site with no build step, no framework, no dependencies beyond two Google Fonts.

## Project structure

```
/
├── index.html              # HTML structure only — no inline CSS or JS
├── css/
│   └── styles.css          # All styles
├── js/
│   └── main.js             # All JavaScript
├── data/
│   └── roles.json          # Role content (source of truth)
├── assets/
│   ├── headshots/          # One JPG per role slug (e.g. dojo.jpg)
│   └── logos/              # One PNG per role slug (e.g. dojo.png)
├── serve.sh                # Local dev server (python3 / python / npx)
└── CLAUDE.md               # This file
```

No package.json, no bundler, no node_modules. Serve locally with `./serve.sh` or any static server.

---

## What the site does

A minimalist personal CV site with two distinct experiences:

**Desktop (>768px)**
- Fixed left panel: bold serif headline + role description + LinkedIn/GitHub links
- Right side: tall scroll container (100vh per role × 6 roles), sections generated dynamically from `roles.json`
- A sticky ID card in the centre-right — flips to reveal each role as you scroll
- Left panel copy fades out and in to match the current role
- Card gently swings and tilts with mouse movement when idle

**Mobile (≤768px)**
- Single-screen layout, no scroll — everything fits within `100dvh`
- Swipe left/right (or tap arrow buttons) to cycle through role cards
- Progress dots + role copy below the card
- LinkedIn/GitHub buttons at the bottom
- Portrait-only: landscape shows a "Please rotate your device" overlay

**Responsive switching** — when the viewport crosses the 768px breakpoint (e.g. browser resize or devtools), the active view is fully torn down and the correct one re-initialises from scratch.

---

## Key design decisions to preserve

- **Always light mode** — `color-scheme: only light` on `html` and `<meta name="color-scheme" content="only light">`. Never add dark mode.
- **Portrait-only on mobile** — landscape is blocked via CSS (`body { display: none }` + overlay at `max-width: 768px and orientation: landscape`).
- **No CSS 3D back-face on the flip** — the flip uses a rotate-to-edge / content-swap / rotate-from-edge technique, not `backface-visibility` or `transform-style: preserve-3d`. Do not revert to a two-face approach — `backface-visibility: hidden` is unreliable when face elements have `overflow: hidden` (creates a stacking context that breaks 3D rendering in some browsers), causing mirrored text to show through.
- **Shadow on a sibling element** — the card shadow lives on `#cardShadow` (a sibling of `#card3d`) using `box-shadow`, not `filter: drop-shadow` on the card itself. `filter` flattens `transform-style: preserve-3d` and causes the shadow to blink during transitions. The shadow is updated every RAF frame so it never blinks.
- **Identical cards on desktop and mobile** — `buildCardInner()` is shared; there are no mobile-specific CSS overrides on card internals. Both views render the same card at the same dimensions.
- **Role sections are generated by JS** — `.role-section` divs are created dynamically in `desktopInit()` from the `roles` array. Do not add hardcoded `.role-section` divs to `index.html`.

---

## Colour palette

```css
--black: #0d0d0d
--white: #fafaf8   /* site background — warm off-white, not pure white */
--grey:  #888
--light: #e8e8e4
```

Each role card has its own `--accent` colour (top stripe, job title text, company icon background):

| Role | Company | Accent |
|------|---------|--------|
| 0 | Hartree Partners | `#0d2358` |
| 1 | DOJO | `#1a1a2e` |
| 2 | Hummingbird | `#1e4d2b` |
| 3 | Think Big | `#c84b00` |
| 4 | Big Data Partnership | `#7c2d12` |
| 5 | Mu Sigma | `#7a1a1a` |

---

## Typography

- **Display / headings**: `Instrument Serif` (Google Fonts) — left-panel headline and card name
- **Body / UI**: `DM Sans` (Google Fonts) — everything else
- Never change these to Inter, Roboto, Arial, or system fonts.

---

## Roles data

All role content lives in `data/roles.json`. Each object has:

```json
{
  "company":  "string",   // shown on card label and mobile eyebrow
  "title":    "string",   // job title on card
  "dates":    "string",   // date range on card and mobile eyebrow
  "accent":   "#rrggbb",  // hex colour for card accent elements
  "icon":     "string",   // single char / emoji — fallback if logo fails to load
  "headline": "string",   // desktop left panel — use \n for line breaks
  "eyebrow":  "string",   // small uppercase label above headline
  "sub":      "string",   // one-sentence description below headline
  "headshot": "string",   // path to headshot image (e.g. assets/headshots/dojo.jpg)
  "logo":     "string"    // path to company logo PNG (e.g. assets/logos/dojo.png)
}
```

Roles are in reverse-chronological order (most recent first). Every card shows its `headshot` image with an `onerror` fallback to the `👤` placeholder emoji — there is no "photo only on first card" logic.

---

## Card dimensions

```css
--card-w: 260px
--card-h: 412px   /* ISO/IEC 7810 ID-1 portrait: 85.60×53.98mm */
```

The `.id-card-wrap` has both `width` and `height` set to these values — the height is required so the `#cardShadow` sibling (which uses `position: absolute; inset: 0`) has dimensions to fill.

Mobile cards use `height: min(var(--card-h), 56dvh)` with `aspect-ratio: 53.98 / 85.60` so the height is the primary constraint and width is derived. The headshot photo is `130px × 130px` on both desktop and mobile.

---

## Card flip mechanism (desktop)

`rotateTo(idx)` in `main.js` works in two phases with no fading:

1. **Rotate out to edge-on**: `perspective(600px) rotateY(±90deg) rotate(-1deg)` over 0.2s with `cubic-bezier(0.4, 0, 1, 1)`. At exactly 90° the card has zero visible width — it is completely invisible to the viewer.
2. **Content swap**: while the card is edge-on, `cardFront.innerHTML` is replaced with the new role's content. The card is instantly snapped to `rotateY(∓90deg)` (opposite edge, still invisible).
3. **Rotate in from edge**: animates to `perspective(600px) rotateY(0deg) rotate(-2deg)` over 0.28s with `cubic-bezier(0, 0, 0.2, 1)`.

The viewer sees a continuous 180° arc. The content swap is imperceptible because it happens at the zero-width edge-on moment.

**Why not `backface-visibility` / `preserve-3d`**: These CSS 3D properties are unreliable when face elements have `overflow: hidden` (which creates a stacking context that interferes with 3D rendering), causing the front face's backside to bleed through as mirrored text during the 90°–180° phase.

Flip direction (±90deg exit side) is determined by comparing the new index to `prevRole` captured before `currentRole` is updated. An `animating` flag blocks new flips mid-animation.

---

## Idle swing + mouse tilt (desktop)

`animTick()` runs every RAF frame:

- `swingPhase` increments by `0.007` per frame, driving a sine wave: `Math.sin(swingPhase) * 2 - 1` degrees of in-plane rotation (`rotate()`).
- Mouse position is normalised to `[0, 1]` in both axes. `tiltX` and `tiltY` lerp toward mouse-position targets (`±12°` X, `±18°` Y) at `0.09` per frame.
- When `animating = true`, the transform is not updated by the RAF loop (the CSS transition owns the card).
- Shadow on `#cardShadow` is updated every frame regardless of `animating` — shadow never blinks during a flip.

---

## Scroll detection (desktop)

Uses `IntersectionObserver` with `rootMargin: '-50% 0px -50% 0px'` — a section triggers when its centre crosses the viewport midpoint. Fires `rotateTo(idx)` when a new section intersects.

---

## Mobile carousel

Cards are individually `position: absolute` inside `#mobileSwipeArea`, each centred at `top: 50%; left: 50%`. JS sets `translateX(calc(-50% + Xpx)) translateY(-50%)` where `X = (i - current) * slotW + dragOffset`.

- Normal navigation: `cubic-bezier(0.25, 1, 0.5, 1)` ease-out
- End-of-carousel spring-back: `cubic-bezier(0.34, 1.56, 0.64, 1)` overshoot
- Dragging past first/last card applies `× 0.18` rubber-band resistance
- Flick detection threshold: `flickVel > 3` (px/frame at 60fps)
- Drag commit threshold: `offsetX > slotW * 0.25`

---

## Responsive init / teardown

Both `desktopInit(roles)` and `mobileInit(roles)` return a `destroy()` function. The bootstrap uses `matchMedia('(max-width: 768px)')` and listens for `change` events:

```js
let destroy = mq.matches ? mobileInit(roles) : desktopInit(roles);
mq.addEventListener('change', e => {
  destroy();
  destroy = e.matches ? mobileInit(roles) : desktopInit(roles);
});
```

Each `destroy()` uses `AbortController.abort()` to remove all event listeners, cancels RAF / disconnects observers, and clears dynamically created DOM.

---

## Mobile layout constraints

The mobile layout must fit entirely within `100dvh` (no scroll):
- `.mobile` is `height: 100dvh; overflow: hidden`
- `.mobile-swipe-area` uses `flex: 1; min-height: 0` to fill remaining space
- Role copy is clamped to 2 lines with `-webkit-line-clamp: 2`
- Bottom links use `env(safe-area-inset-bottom)` for iPhone home indicator

Do not add `min-height` to any mobile flex children — it will break the no-scroll constraint.

---

## Links

- LinkedIn: `https://linkedin.com/in/austincv`
- GitHub: `https://github.com/austincv`

These appear in the desktop left panel and the mobile bottom buttons. Do not add other links without being asked.

---

## What's intentionally absent

- No analytics, no tracking
- No contact form
- No blog / writing section (yet)
- No dark mode
- No landscape mobile view
